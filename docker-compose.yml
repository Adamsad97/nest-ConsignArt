services:
  node:
    image: node:26.2.0-alpine3.23
    user: node
    tty: true
    working_dir: /home/node/app
    volumes:
      - .:/home/node/app
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm run start:dev"